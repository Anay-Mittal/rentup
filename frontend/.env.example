# Copy to .env.production and fill in real backend URL before running `npm run build`
# Or set this in Netlify dashboard: Site settings → Environment variables
REACT_APP_API_URL=https://your-backend.onrender.com
